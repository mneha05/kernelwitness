# KernelWitness docs

The documentation site is entirely static. `docs/demo/index.html` is generated by `kernelwitness demo` and can be hosted on GitHub Pages, Railway, Netlify, S3, or opened directly from disk.
